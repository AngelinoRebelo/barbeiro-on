services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: barber
      POSTGRES_PASSWORD: barber
      POSTGRES_DB: barber
    ports:
      - "5432:5432"
    volumes:
      - barber_pg:/var/lib/postgresql/data

volumes:
  barber_pg:
